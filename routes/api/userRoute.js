/* const express = require('express');
const authControler = require('./../../controlers/authControler');

const router = express.Router();

router.post('/', authControler.signup);

module.exports = router; */